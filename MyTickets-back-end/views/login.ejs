<!DOCTYPE html>
<html>
<head>
      <title>WebETkd || Login for Admin</title>
      <%- include('./include/head'); %>
  </head>
  
<body class="bodyimg">
    <div class="backgroundcolor" align="center">
        <%  
            if(message == "loginSuccess") { %>
                <div class="alert alert-success">
                    <%= login_success %>
                </div>
            <% } else if(message == "loginError") { %>
                <div class="alert alert-danger">
                    <%= login_err %>
                </div>
            <% } else if(message == "logout") { %>
                <div class="alert alert-success">
                    <%= logout_success %>
                </div>
            <% } else if(message == "notLogin") { %>
            <% } %>
        <div class="gridspace1">
                <div class="left-grid">
                    <img src="/images/image1.jpg" style="width: 250px; height: 140px; margin-top: 120px; margin-left: 50px;">
                </div>
                <div class="mid-grid">
                    <div class="vl"></div>
                </div>
                <div class="right-grid" align="left">
                <form method="POST" action="/">
                    <div class="H1" align="left"><br>Login Menu for My Tickets System</div><p></p><br>
                    <!--input username-->
                    <input type="text" class="btn" placeholder="Username" name="username" id="username" rnodeequired><p></p>
                    <!--input password-->
                    <input type="password" class="btn" placeholder="Password" name="password" id="password" required><p></p>
                    <!--เรียกใช้ฟังก์ชั่น Login ยังไม่ได้ทำ-->
                    <button type="submit" class="submitbtn" name="submit_button" id="submit_button" value="Login">Login</button><p></p>
                    <div class="gridspace4">
                        <div class="left-grid">
                            <a href="/register" class="H2">Register</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html>